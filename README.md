# AoT Idle Server Magic

* POST | /users | used to create a new user in the DB
* POST | /login | used to validate an existing user in the DB
* GET | /users | used to return user information from the DB
